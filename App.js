import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

import ListDeviceScreens from './screens/listDevices/index';

const App = () => {
  return <ListDeviceScreens />;
};

export default App;
